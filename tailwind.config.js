/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./public/**/*.{html,js}', './src/**/*.{jsx,js}'],  // make sure it scans both HTML and JS files
  theme: {
    extend: {},
  },
  plugins: [],
}
